/* Scaffolding: prop unions live next to the component they belong to. Delete with DemoButton. */

export type ButtonSize = 'sm' | 'md' | 'lg';
export type ButtonVariant = 'primary' | 'secondary' | 'ghost';
